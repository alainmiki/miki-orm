[tasks]
install = "python -m pip install -U pip && pip install -e .[dev]"
lint = "python -m ruff check myorm tests"
test = "pytest"
docs = "mkdocs build"
makemigrations = "python -m myorm.migrations.engine --makemigrations"
migrate = "python -m myorm.migrations.engine --migrate"
