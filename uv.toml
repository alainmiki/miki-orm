[tasks]
install = "python -m pip install -U pip && pip install -e .[dev]"
lint = "python -m ruff check mikiorm tests"
test = "pytest"
docs = "mkdocs build"
makemigrations = "python -m mikiorm.migrations.engine --makemigrations"
migrate = "python -m mikiorm.migrations.engine --migrate"
