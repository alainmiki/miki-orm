Metadata-Version: 2.4
Name: miki-orm
Version: 0.1.0
Summary: Universal Django-style ORM for Python with SQLite, PostgreSQL, and MySQL support.
Project-URL: Documentation, https://example.com/docs
Project-URL: Homepage, https://example.com
Project-URL: Repository, https://github.com/your-org/miki-orm
Requires-Python: >=3.14
Description-Content-Type: text/markdown
Requires-Dist: psycopg2-binary<3.0,>=2.9
Requires-Dist: pymysql<2.0,>=1.0
Requires-Dist: PyYAML>=6.0
Requires-Dist: python-dotenv>=1.1
Requires-Dist: typing_extensions>=4.8
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: mkdocs>=1.5; extra == "dev"
Requires-Dist: ruff>=0.0; extra == "dev"
Requires-Dist: black>=24.3; extra == "dev"

# miki-orm

Universal Django-style ORM for Python applications.

## Repository layout

- `myorm/` — ORM package with Django-style models, fields, managers, migrations, and adapters.
- `tests/` — Unit and integration test stubs.
- `docs/` — Product and technical documentation.
- `.github/workflows/ci.yml` — GitHub Actions CI.
- `uv.toml` — `uv` task runner commands.

## Features

- Django-like model fields and manager methods.
- `makemigrations` and `migrate` command structure.
- Sync support for SQLite, PostgreSQL, and MySQL.
- Universal config style for database credentials and settings.
- Production-minded security and scalability patterns.

## Quickstart

1. Install with uv:

```bash
uv sync --extra dev
```

2. Configure your database:

```python
from myorm import configure

configure({
    "database": {
        "engine": "sqlite",  # or "postgres", "mysql"
        "name": "mydb.db"
    }
})
```

3. Define a model:

```python
from myorm import Model, IntegerField, CharField

class User(Model):
    id = IntegerField(primary_key=True, auto_increment=True)
    name = CharField(max_length=100)
    age = IntegerField()

    class Meta:
        table_name = "users"
```

4. Generate and apply migrations:

```bash
makemigrations
migrate
```

5. Use the model:

```python
user = User(name="Alice", age=30)
user.save()

users = User.objects.filter(age__gt=25)
```

## Development

Run tests:

```bash
uv run pytest tests/
```

Lint:

```bash
uv run ruff check . && uv run black --check .
```

Build docs:

```bash
uv run mkdocs build
```
```

## Configuration

Use a Django-like `DATABASES` dictionary in your application or environment loader.

Example config:

```python
DATABASES = {
    "default": {
        "ENGINE": "postgresql",
        "NAME": "mydb",
        "USER": "user",
        "PASSWORD": "password",
        "HOST": "localhost",
        "PORT": 5432,
        "OPTIONS": {"sslmode": "require"},
    }
}
```

## Documentation
- `docs/PRD.md`
- `docs/TRD.md`

## CI
GitHub Actions CI is configured in `.github/workflows/ci.yml` and validates SQLite, PostgreSQL, and async scenarios.
